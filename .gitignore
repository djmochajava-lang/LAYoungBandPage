# Jekyll
_site
.sass-cache
.jekyll-cache
.jekyll-metadata

# Dependencies
# Root-only (anchored, Jekyll/bundler dir). js/vendor/ (self-hosted
# version-pinned public SDKs, e.g. supabase-js — story-supabase-js-sri-pin)
# IS tracked and deploys to Pages.
/vendor/
node_modules/

# Environment & secrets (SEC-002)
.env
.env.local
.env.*.local
*.key
*.pem
*.p12
*.pfx
credentials.json
secrets.json
service-account.json
serviceAccountKey.json

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db
._*

# Project-specific
project-code-dump.txt
Video/big/
# Captured YouTube masters (tools/capture-videos.py) — hosted on Supabase artist-video, not in git
Video/live/
Video/archive/*
!Video/archive/index.json
nul
# Cover masters are served from isolated storage, not this repo.
Media/*.mp3
